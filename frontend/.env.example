# Base URL of the NestJS backend (used for REST calls from the browser).
NEXT_PUBLIC_API_URL="http://localhost:4000/api"

# Base URL of the Socket.IO server (usually same host as backend, no /api suffix).
NEXT_PUBLIC_SOCKET_URL="http://localhost:4000"
